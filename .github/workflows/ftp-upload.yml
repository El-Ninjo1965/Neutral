name: FTPS Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    if: ${{ github.event_name == 'workflow_dispatch' || !contains(github.event.head_commit.message, '[cleanup-public-html]') }}
    runs-on: ubuntu-latest
    env:
      FTP_HOST: ${{ secrets.FTP_HOST }}
      FTP_USER: ${{ secrets.FTP_USER }}
      FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
      FTP_PORT: ${{ secrets.FTP_PORT || 21 }}
      FTP_TARGET_DIR: ${{ secrets.FTP_TARGET_DIR || '/' }}
      FTP_SSL_CHECK_HOSTNAME: ${{ secrets.FTP_SSL_CHECK_HOSTNAME || 'true' }}

    steps:
      - name: Repository auschecken
        uses: actions/checkout@v4

      - name: Produktions-Staging aufbauen
        shell: bash
        run: |
          set -euo pipefail

          STAGING_DIR="${RUNNER_TEMP}/neutral-ftps-staging"
          rm -rf "$STAGING_DIR"
          mkdir -p "$STAGING_DIR"

          copy_dir() {
            local source_path="$1"
            local target_path="$STAGING_DIR/$1"
            mkdir -p "$target_path"
            cp -a "$source_path"/. "$target_path"/
          }

          # Production consists only of the browser Web-App and PHP server.
          # The Node adapter remains development/test-only and is not deployed.
          copy_dir Web-App
          copy_dir Server/php
          copy_dir Server/public
          cp .htaccess "$STAGING_DIR/.htaccess"

          test -f "$STAGING_DIR/Web-App/public/index.html"
          test -f "$STAGING_DIR/Web-App/app/modules/gps/module.json"
          test -f "$STAGING_DIR/Server/public/api/index.php"
          test -f "$STAGING_DIR/Server/php/bootstrap.php"
          echo "Staging tree:"
          find "$STAGING_DIR" -type f | sort

      - name: FTPS-Client bereitstellen
        shell: bash
        run: |
          set -euo pipefail

          if ! command -v lftp >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y lftp
          fi

      - name: Produktionsbestand per FTPS hochladen
        shell: bash
        run: |
          set -euo pipefail

          STAGING_DIR="${RUNNER_TEMP}/neutral-ftps-staging"

          lftp <<EOF
          set cmd:fail-exit true
          set ftp:ssl-force true
          set ftp:ssl-protect-data true
          set ssl:check-hostname $FTP_SSL_CHECK_HOSTNAME
          set ssl:verify-certificate true
          set net:timeout 30
          set net:max-retries 1
          open -u "$FTP_USER","$FTP_PASSWORD" -p "$FTP_PORT" "$FTP_HOST"
          mirror -R --only-newer --verbose \
            --exclude-glob .env \
            "$STAGING_DIR" "$FTP_TARGET_DIR"
          bye
          EOF

      - name: Produktionsnachweis sichern und Testartefakte entfernen
        shell: bash
        run: |
          set -euo pipefail

          RESULT_FILE="${RUNNER_TEMP}/neutral-health-result.json"
          PUBLIC_DIR="${FTP_TARGET_DIR%/}/Server/public"

          lftp <<EOF
          set cmd:fail-exit true
          set ftp:ssl-force true
          set ftp:ssl-protect-data true
          set ssl:check-hostname $FTP_SSL_CHECK_HOSTNAME
          set ssl:verify-certificate true
          set net:timeout 30
          set net:max-retries 1
          open -u "$FTP_USER","$FTP_PASSWORD" -p "$FTP_PORT" "$FTP_HOST"
          get "$PUBLIC_DIR/health-result-91c4e7a2.json" -o "$RESULT_FILE"
          rm "$PUBLIC_DIR/health-91c4e7a2.php"
          rm "$PUBLIC_DIR/health-result-91c4e7a2.json"
          set cmd:fail-exit false
          rm "$PUBLIC_DIR/neutral-db-inspect-91c4e7a2.php"
          rm "$PUBLIC_DIR/neutral-db-result-91c4e7a2.json"
          rm "$PUBLIC_DIR/neutral-db-result-91c4e7a2.php"
          rm "$PUBLIC_DIR/neutral-db-reset-91c4e7a2.php"
          rm "$PUBLIC_DIR/neutral-db-action-91c4e7a2.php"
          rm "$PUBLIC_DIR/neutral-e2e-91c4e7a2.php"
          rm "$PUBLIC_DIR/neutral-db-reset-result-91c4e7a2.php"
          bye
          EOF

          jq -e '
            .marker == "neutral-health-91c4e7a2" and
            (.identity_verified == true) and
            (.database_connected == true) and
            (.table_set_verified == false) and
            (.table_count == 16) and
            (.setup_status == "ACTIVE") and
            (.migration_count == 2) and
            (.error_category == null) and
            (.result_persisted == true)
          ' "$RESULT_FILE" >/dev/null
          jq '{marker, identity_verified, database_connected, table_count, setup_status, migration_count, error_category, result_persisted}' "$RESULT_FILE"
